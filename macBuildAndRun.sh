#!/bin/zsh
# This is a temporaty workaround so i can "build" in clion" on mac
echo "Warning permormance may be impaced by clion"
echo "if so switch from the apps running window back to clion and then back"
./macbuild.sh
./MacWindow
